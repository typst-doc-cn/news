#import "/typ/templates/news.typ": *

#show: news-template.with(
  date: "2024-12-17",
  title: "PR #5581 Fix sticky blocks at the top of blocks and pages",
  lang: "en",
  tags: ("pr",),
  description: "This PR fixes an issue where `sticky` `blocks` at the top of `blocks` or `pages` were not sticking.",
)

Fixed an issue where `sticky` `blocks` at the top of `blocks` or `pages` were not sticking properly.
