
#import "/typ/templates/news.typ": *

#show: news-template.with(
  date: "2024-12-17",
  title: "PR #5581 Fix sticky blocks at the top of blocks and pages",
  lang: "zh",
  region: "CN",
  tags: ("pr",),
  description: "此 PR 修复了在 `block` 或 `page` 顶部的 `sticky` `block` 不 `sticky` 的问题.",
)
修复了在 `block` 或 `page` 顶部的 `sticky` `block` 不 `sticky` 的问题.


